# SSH connection used by scripts/connect_vm.sh.
OPENPI_VM_SSH_HOST=YOUR_GPU_VM_IP
OPENPI_VM_SSH_USER=root
OPENPI_VM_IDENTITY_FILE=~/.ssh/openpi_vm

# The Mac client connects to the local side of this SSH tunnel.
OPENPI_VM_LOCAL_HOST=127.0.0.1
OPENPI_VM_LOCAL_PORT=8000
OPENPI_VM_REMOTE_PORT=8000
